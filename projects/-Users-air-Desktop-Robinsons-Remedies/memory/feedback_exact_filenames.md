---
name: Use exact filenames user provides
description: When user gives a video/file name, match it exactly — don't grab a similar-sounding file
type: feedback
---

When the user specifies a filename, match it exactly. Do not grab a file with a similar name.

**Why:** User said "stickbuggedagain" and I uploaded `StickBugged_YouTube.mov` instead of `stickbuggedagain.mov`. Wrong video entirely.

**How to apply:** Search for the exact string the user gives. If multiple results come back, pick the exact match, not a partial match.
