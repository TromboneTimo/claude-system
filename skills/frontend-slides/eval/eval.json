{"assertions":["Output must NOT contain em dashes","Output is a single self-contained HTML file","Every slide has height 100vh or 100dvh","Output uses clamp() for font sizes","No slide repeats the same layout as the previous slide","Each slide has MAX 2-3 sentences","Must NOT fabricate quotes for real people","Must NOT use Wikipedia/Wikimedia image URLs","All image URLs verified before declaring done"]}
